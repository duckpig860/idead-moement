const Gimkit = require('gimkit-flooder.js');

const gimkit = new Gimkit();

gimkit.setPin(set pin here);

gimkit.on('joined', player => {
    console.log(`${player} joined`);
});

gimkit.on('error', error => {
    console.log(error);
});

gimkit.join('Sean V');
